{
  "name": "fontsense",
  "version": "1.0.0",
  "description": "For an input csv of URLs the script creates an output files with the font files in use",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Devyandu",
  "license": "ISC",
  "dependencies": {
    "csv-parser": "^3.1.0",
    "csv-writer": "^1.6.0",
    "puppeteer": "^24.1.0"
  }
}
